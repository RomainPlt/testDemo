version: '3'
services:
        demo:
                image: romainplt/demo
                ports:
                 - "8080:8080"
                command: java -jar /build/libs/demo-0.0.1-SNAPSHOT.jar
                devices:
                 - /dev/isgx:/dev/isgx
                environment:
                 - SCONE_LOG=7
                 - SCONE_MODE=HW
                
